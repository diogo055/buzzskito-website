# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/

# Production
/build

# Env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Google Indexing API — service account key (NEVER commit)
scripts/google-service-account.json

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
Thumbs.db

# Claude Code build/run artifacts
build.log
start.log

# Raw GSC API dumps — regenerable, 12-16MB each, and they bloat history permanently.
# The analysis scripts alongside them are kept; re-pull the data when needed.
seo-audit/**/gsc_full_scan.json
seo-audit/**/gsc_by_country.json

# Large cached GSC/Ahrefs pulls (Stage A caches)
data/gsc/*.json
data/gsc/*.csv
